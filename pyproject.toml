[project]
name = "research"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "donut",
    "matplotlib>=3.11.1",
    "pandas>=3.0.3",
]

[tool.uv.sources]
donut = { path = "../donut" }
